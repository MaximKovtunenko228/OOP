public interface имя {
    тип_результата имя_метода1(список параметров);
    ...
    тип_результата имя_методаN(список параметров);
    final тип имя_переменной1 = значение;
    final тип имя_переменнойN = значение;
}
class имя_класса [extends суперкласс]
        [implements интерфейс0 [, интерфейс...]]
        {
                //тело класса
        }
