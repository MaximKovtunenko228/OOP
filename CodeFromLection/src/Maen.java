public class Maen {
    public static void main(String[] args) {
        // тут типо какой-то код
        Body []b = new Body[k];
        Triangle1 tr = new Triangle1(a, b, c);
        b[i] = tr;
        Circle cir = new Circle(r);
        b[i] = cir;
        b[i].print();
        System.out.println("S= " + b[i].S() + " " + "V= " + b[i].P());
    }
}
